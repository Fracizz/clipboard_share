# ClipboardShare tray UI

Tauri 2 shell for ClipboardShare. Depends on the root `clipboard_share` crate.

```powershell
npm install
npm run dev
# release binary via workspace:
cargo build --release -p clipboard_share_ui
```
